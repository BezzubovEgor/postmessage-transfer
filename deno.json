{
  "name": "@lynkxy/postmessage-transfer",
  "description": "A package for sending objects via post message with support for serializing messages with proxy functions and sending them via post message channel across all major platforms including web, Node.js, Deno, etc.",
  "version": "0.0.2",
  "license": "MIT",
  "exports": "./src/index.ts",
  "tasks": {
    "test": "deno run --watch --allow-all src/__test__/main.ts",
    "build:npm": "deno run -A scripts/build-npm.ts",
    "publish:npm": "deno run build:npm && cd npm && npm publish --dry-run"
  },
  "imports": {
    "@deno/dnt": "jsr:@deno/dnt@^0.9.0",
    "@std/assert": "jsr:@std/assert@1"
  }
}
